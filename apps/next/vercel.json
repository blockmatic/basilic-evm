{
  "ignoreCommand": "bash -c 'if [ -z \"$VERCEL_GIT_PREVIOUS_SHA\" ]; then exit 1; fi; if git diff --name-only \"$VERCEL_GIT_PREVIOUS_SHA\" \"$VERCEL_GIT_COMMIT_SHA\" -- apps/next package.json packages pnpm-lock.yaml | grep -q .; then exit 1; else exit 0; fi'",
  "buildCommand": "cd ../.. && pnpm build --filter=@repo/next",
  "devCommand": "cd ../.. && pnpm dev --filter=@repo/next",
  "installCommand": "cd ../.. && pnpm install",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "env": {
    "NODE_ENV": "production"
  },
  "git": {
    "deploymentEnabled": {
      "main": true,
      "develop": true
    }
  }
}
